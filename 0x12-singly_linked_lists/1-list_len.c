#include "list.h"


/**
* list-len - return the lenght of element in a linked list.
* @h: points to the lists
* return number of nodes
**/

size_t list_len(const list_t *h)
{
	size-t cont = o;
	while (h)
		cont++;
h = h->next;
return (cont);
}
